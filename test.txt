"use client";

import { motion } from "framer-motion";
import { useEffect, useState } from "react";
import Image from "next/image";

export default function HeroSection() {
  const [cursor, setCursor] = useState({ x: 50, y: 50 });

  // Track mouse position for spotlight
  useEffect(() => {
    const handleMove = (e: MouseEvent) => {
      setCursor({
        x: (e.clientX / window.innerWidth) * 100,
        y: (e.clientY / window.innerHeight) * 100,
      });
    };
    window.addEventListener("mousemove", handleMove);
    return () => window.removeEventListener("mousemove", handleMove);
  }, []);

  return (
    <section className="relative h-screen flex items-center justify-center text-white overflow-hidden">
      {/* 🎨 Animated Gradient Glow */}
      <motion.div
        className="absolute inset-0"
        style={{
          background:
            "linear-gradient(135deg, #1DB954 0%, #191414 50%, #3D3D3D 100%)",
          backgroundSize: "200% 200%",
        }}
        animate={{ backgroundPosition: ["0% 50%", "100% 50%", "0% 50%"] }}
        transition={{ duration: 15, repeat: Infinity, ease: "linear" }}
      />

      {/* 🎶 Pulsing Glow Overlay */}
      <motion.div
        className="absolute inset-0 bg-gradient-to-r from-green-500/20 via-purple-500/20 to-black/70"
        animate={{ opacity: [0.6, 0.9, 0.6] }}
        transition={{ duration: 2, repeat: Infinity, ease: "easeInOut" }}
      />

      {/* 📊 Equalizer Bars */}
      <div className="absolute bottom-0 flex w-full h-40 items-end justify-center gap-1">
        {Array.from({ length: 30 }).map((_, i) => (
          <motion.div
            key={i}
            className="w-1 bg-green-400/40 rounded-t"
            animate={{
              height: [10, 80, 30, 60, 20],
            }}
            transition={{
              duration: 1 + Math.random(),
              repeat: Infinity,
              ease: "easeInOut",
              delay: i * 0.1,
            }}
          />
        ))}
      </div>

      {/* ✨ Cursor Spotlight */}
      <div
        className="absolute inset-0 pointer-events-none"
        style={{
          background: `radial-gradient(circle at ${cursor.x}% ${cursor.y}%, rgba(29,185,84,0.25), transparent 40%)`,
        }}
      />

      {/* Dark overlay for readability */}
      <div className="absolute inset-0 bg-black/40" />

      {/* 🔥 Hero Content */}
      <div className="relative z-10 text-center px-6">
        {/* Example "Now Playing" card */}
        <motion.div
          className="bg-neutral-900/80 backdrop-blur-md rounded-2xl shadow-lg p-6 inline-flex items-center gap-4"
          initial={{ opacity: 0, y: 30 }}
          animate={{ opacity: 1, y: 0 }}
          transition={{ duration: 1 }}
        >
          <Image
            src="/assets/album-art.jpg"
            alt="Album Art"
            width={80}
            height={80}
            className="rounded-md"
          />
          <div className="text-left">
            <h3 className="text-lg font-semibold">Now Playing</h3>
            <p className="text-sm text-neutral-400">Artist Name</p>
          </div>
        </motion.div>

        <h1 className="mt-10 text-5xl md:text-6xl font-bold">
          Visuals that <span className="text-green-500">Resonate</span>
        </h1>
        <p className="mt-4 text-lg text-neutral-300 max-w-xl mx-auto">
          A Spotify-inspired portfolio showcasing films, visual art, and creative
          storytelling.
        </p>
      </div>
    </section>
  );
}


// new line


"use client";

import { motion } from "framer-motion";
import Image from "next/image";

export default function VintageHero() {
  return (
    <section className="relative h-screen w-full overflow-hidden bg-black text-white">
      {/* Background Image */}
      <Image
        src="/assets/vintage-bg.jpg"
        alt="Vintage Background"
        fill
        className="object-cover object-center opacity-80"
        quality={100}
      />

      {/* Grain Overlay */}
      <div className="absolute inset-0 bg-black/20 pointer-events-none">
        <div className="w-full h-full bg-[url('/assets/grain.png')] opacity-30 mix-blend-overlay" />
      </div>

      {/* Warm Gradient Overlay */}
      <div className="absolute inset-0 bg-gradient-to-t from-black via-black/80 to-transparent" />

      {/* Hero Content */}
      <div className="relative z-10 container mx-auto px-6 flex flex-col items-center justify-center h-full text-center">
        <motion.h1
          className="text-6xl md:text-8xl font-serif font-bold leading-tight"
          initial={{ opacity: 0, y: -50 }}
          animate={{ opacity: 1, y: 0 }}
          transition={{ duration: 1 }}
        >
          <span className="text-amber-300">Cinematic</span> Visions
        </motion.h1>

        <motion.p
          className="mt-6 max-w-xl text-lg md:text-xl text-neutral-300"
          initial={{ opacity: 0 }}
          animate={{ opacity: 1 }}
          transition={{ delay: 0.5, duration: 1 }}
        >
          A curated collection of films and visual art. Explore a world of
          story, mood, and timeless style.
        </motion.p>

        <motion.button
          className="mt-8 px-8 py-4 bg-amber-700/80 rounded-full font-semibold text-lg hover:bg-amber-600 transition"
          initial={{ opacity: 0 }}
          animate={{ opacity: 1 }}
          transition={{ delay: 1 }}
        >
          View Portfolio
        </motion.button>
      </div>

      {/* Subtle Flicker Effect */}
      <motion.div
        className="absolute inset-0 bg-black/10 pointer-events-none"
        animate={{ opacity: [0.05, 0.1, 0.05] }}
        transition={{ duration: 2, repeat: Infinity, ease: "easeInOut" }}
      />
    </section>
  );
}


     {/* Social Links */}
          <div className="flex flex-col items-center space-y-6">
            {[Instagram, Youtube, Mail].map((Icon, index) => (
              <motion.button
                key={index}
                className="text-white/60 cursor-pointer hover:text-white transition-colors duration-300"
                initial={{ opacity: 0, scale: 0 }}
                animate={{ opacity: 1, scale: 1 }}
                transition={{ delay: 0.8 + index * 0.1 }}
                whileHover={{ scale: 1.2, y: -3 }}
                whileTap={{ scale: 0.9 }}
              >
                <Icon className="w-5 h-5" />
              </motion.button>
            ))}
          </div>
        </div>